# 第二批结构调整

> 状态：已完成。本文保留任务拆分与验收记录；当前入口见[技术文档索引](../../README.md)。

基线：876e158f。范围限于已确认的三项：命令层收拢、协议分域、Workbar 控制器拆分。

- [x] A｜命令层：退役只由测试调用的旧执行注册器，保留必要解析/补全；按领域组织正式 RPC 命令，将记忆状态/撤销对齐现有原子行为，迁移有效测试。
- [x] B｜协议：拆分基础定义、传输/信封与领域契约/校验，保留公开导出、版本、方法集合、精确参数/结果验证和注册完备性。
- [x] C｜Workbar：按 Graph、Tasks、Files、Inspector、Review、Terminal 分离完整控制器；Host 保留选择与会话实例身份，终端绑定仍由单一模块拥有。
- [x] 集成｜真实 TUI/daemon 记忆链验收；相关回归、根和 Desktop 类型检查、构建、lint、严格架构检查，复核差异后合并推送并清理临时分支。

A、B、C 使用独立 worktree 与唯一分支；A 只改命令及相关测试，B 独占协议包与协议测试，C 独占面板控制器与对应桌面测试。主代理维护文档与跨模块验收，在独立集成分支合并验证。

协议不改 wire 或版本；Workbar 不改交互、CSS、实例 key、generation 校验和终端关闭顺序。命令层仅修正已退役记忆审核语义，不增加旧注册器中尚未接入正式 TUI 的功能。保留数据库 schema、事务、执行内核、权限和 Provider 并发边界。原工作区未跟踪文章、配图与截图不纳入本次变更。

测试中的有效行为迁移到生产入口验证；只有专门验证已退役执行体的用例才能删除。协议拆分对照原导出与正反解码结果；所有 package 构建和集成测试使用各 worktree 自己的本地 packages。

## 实施结果与验收

| 任务 | 结果                                                                                                 | 分支验证                                                                                         |
| ---- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| A    | 旧注册器及直连记忆命令退役；client-commands 2187 → 141 行，6 个领域模块；纯 undo codec 不依赖 daemon | 39 项相关集成通过；36 个静态命令只有 memory 执行体改变，元数据/顺序不变                          |
| B    | runtime.ts 5400 → 300 行；15 个基础/传输/领域模块                                                    | 44 项相关集成通过；166 导出、305 原声明、127 方法声明及参数/结果表达式一致；14860 次解析差分一致 |
| C    | WorkbarPanelHost 2137 → 58 行；6 个控制器及窄共享模块                                                | 33 项相关集成通过；85 个声明/64 个函数 AST 相同，26 个公开导出不变，构建 CSS 相同                |

新增真实 TUI/daemon 记忆集成测试先在旧代码复现 status 仍显示 Review/Pending 的偏差，合入后完整通过：未信任写入被拒且不创建 memory.sqlite；remember、status、archive undo、off/on 均经过真实协议和原子存储，模型 Run 数量为零。

协议完成一次独立只读复核：运行时值依赖无环、异常类身份不变、方法校验聚合无遗漏或重复，762 次聚合入口与领域校验对照一致。

最终验证：271 个常规集成测试文件递归执行，共 1546 项，1534 项通过、12 项平台条件跳过、0 失败。根与 Desktop 类型检查、根与 Renderer 构建、全仓库 lint、严格架构检查、差异检查和全部相对 Markdown 链接检查通过。没有执行真实模型、Windows 专项或交互浏览器测试。
