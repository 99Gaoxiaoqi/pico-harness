# Computer Use 验收问题修复与全新复测

基线：81262995；对应 2026-09-16 隔离环境实测的 10 项问题。

## 分工与验收

- [ ] A：审批/问答终态及时持久化、前端旧快照不能复活已处理交互；侧聊保留已回答历史。
- [ ] B：显式交付物通过真实 write_file 正式登记，生成文件列表、预览、安全打开及另存闭环。
- [ ] C：Graph 成功不被清理状态覆盖、时间线历史状态正确；Git 新文件计数与 diff 一致。
- [ ] 主代理：审阅绑定任务和运行；Automation 失败保留输入；Memory 不消费其他页面错误；添加项目立即可见；用量自动更新。
- [ ] 集成：相关回归、类型检查、架构检查、构建；独立集成分支通过后才更新 main。
- [ ] 全新 CU：新可执行构建、新 PICO_HOME/浏览器数据目录、全新合成 Git 和非 Git 项目，沿用授权 DeepSeek，只发送合成内容。

## 边界与风险

审批、持久化和导出是重点风险；保留权限、会话归属、幂等、路径验证和冲突指纹，不通过隐藏数据或直接注入数据库使测试通过。普通源码写入不自动成为交付物。原用户凭证/项目只读，复制的测试凭证在结束后清除，测试后台进程停止。

子代理只提交/推送任务分支，主代理串行集成。失败时保留集成分支用于定位，不更新目标分支。共享文件 renderer/runtime.ts、公开 contract、IPC 接线由主代理独占；其他文件按任务划分，避免并行共享写入。

TUI 的原生 Terminal CU 访问前次被安全策略禁止，不绕过限制；无法执行的覆盖明确标注。没有外部凭证或危险/系统级操作授权的路径不宣称通过。

## 全新复测重点

1. 批准/拒绝后继续至少一次模型工具步骤，期间无旧卡复活；问答完成后侧聊继承历史正确。
2. 真实模型创建 artifact 交付物，GUI 预览、另存校验字节；普通写入仍进入审阅，不混为产物。
3. 在当前会话及原生菜单进入审阅，选择正确任务/运行，预览 diff/回退；仅对本轮合成文件执行回退。
4. Graph/Swarm 子任务正式成功输出后保持成功，用量无需离开页面更新。
5. 添加新项目无需刷新可见；Automation 拒绝保留表单，切 Memory 不出现无关错误。
6. 补测原已通过主链：MCP、Skill、Memory、待办、终端、浏览器、会话管理、诊断。
