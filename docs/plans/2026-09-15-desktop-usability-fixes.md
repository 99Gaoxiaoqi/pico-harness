# Desktop 真实界面问题修复

基线：`7d7e25ec`。来自 2026-09-15 Computer Use 主流程验收。

## 边界与分工

| 分组     | 问题               | 修复策略                                         | 验收                                 |
| -------- | ------------------ | ------------------------------------------------ | ------------------------------------ |
| 历史记录 | 停止后会话不可恢复 | failed 才携带 error；读投影兼容旧取消记录        | 取消、重新打开、继续发送均成功       |
| 历史记录 | 分叉早期回复被覆盖 | 导入消息使用稳定且独立的投影身份                 | 多轮/多步历史完整，重试不重复        |
| 实时消息 | 推理重复、跨轮残留 | 对齐实时与持久身份，终态和迟到帧有明确边界       | 成功/失败/取消均无重复或旧流复活     |
| 桌面面板 | PTY 乱码与重绘残片 | 使用 xterm.js 解释控制序列，按实际字符格同步尺寸 | zsh、ANSI、回车/清行、增量与重连正确 |
| 桌面面板 | Toast 撑满工作栏   | 清除会话页冲突的 bottom 定位                     | 提示紧凑且不覆盖整栏                 |

历史记录、实时消息、桌面面板由三个独立任务分支并行实现；公共协议版本归历史记录所有者，manifest/锁文件和跨域版本 fixture 由集成分支单一所有者修改。

终端采用已有开源终端实现而非自制 VT 解析器：固定 `@xterm/xterm@6.0.0`、`@xterm/addon-fit@0.11.0`，集成测试使用 `@xterm/headless@6.0.0`。参考 [xterm.js 官方说明](https://github.com/xtermjs/xterm.js)。保持已有命令输入框与审批边界，不扩展权限。

## 数据保护

- 不直接修改用户数据库或删除历史。
- 复用已有投影版本机制，按会话需要惰性重建可派生的读投影；原始事件与消息保留。
- 旧数据先在测试/副本验证；避免引入额外 schema 或每次读取全库重建。
- 独立集成分支通过验证后再更新 main，不覆盖用户工作区修改；回退采用新的 revert 提交，不改写共享历史。

## 未证实问题

浏览器关闭与追踪详情期间的全局错误暂不按组件名称猜测补丁。先确认取消坏记录是否导致后台订阅重试持续报错，再用修复后的实际界面复测。一次 Provider 请求失败仅有脱敏通用信息，不宣称已确认模型接入缺陷。

## 执行清单

- [x] 三组最小集成复现及修复
- [x] 旧投影兼容、跨域 fixture 与依赖集成
- [x] 类型、边界、相关集成及 Desktop 构建验证
- [x] 独立环境 Computer Use 复测停止/分叉/终端/提示/浏览器/追踪
- [ ] 检查最终差异、更新目标分支并清理本轮临时工作区

## 2026-09-16 真实界面复测

在隔离 PICO_HOME 与 Electron profile 中使用旧测试会话副本，未复制模型凭据。
新消息使用本机确定性模拟 Provider；真实界面验证不等同于外部模型服务质量验证。

- 已通过旧取消会话恢复、旧分叉早期回复、新分叉两轮保留、停止后重开及继续发送。
- 已通过终端 ANSI 颜色/回车清行、浏览器两次加载/清空、追踪详情读取，以及实际 Toast 紧凑布局。
- 源库原始事件摘要未变化；副本旧分叉惰性重建为 v6，非法取消 error 清零，原历史事件保留。
- 发现失败轮推理仍可在后续轮次复活：host 订阅 owner 未在运行终态关闭内存流，重新打开快照又合入旧流。继续修复该真实生产链路，再次打包和界面复验。
- 集成基线全量测试 1770 通过、12 跳过、0 失败；暂停态重试补充后定向回归 19 通过。根类型、lint/架构边界、format 均通过。Desktop 打包因下载瞬时失败重试一次后通过。

### 最终验收

- 宿主终态清理与退役流身份修复已合入，生产链路回归在修复前稳定复现、修复后通过。
- 最终代码 `4c420636` 重新打包并重启独立应用；Computer Use 再次执行失败、成功恢复、慢流停止、切换重开，旧失败推理没有复活，合法回复与历史正常。
- 最终受影响集成回归 43/43 通过，根类型检查、全局 lint/架构边界、format、Desktop 构建通过。
- 浏览器与追踪先前的全局错误本轮未复现，不据此声明已确认独立组件根因；外部 Provider 一次瞬时失败没有被模拟模型验证替代。
- 本轮未覆盖所有外部 MCP、Git 写入、定时任务和真实模型组合，不将重点路径验收表述为整个项目无缺陷。
