{
  "name": "@pico/runtime-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runtime host kernel + connection skeleton (transport/control/protocol/server/client) ported from the runtime-host pattern. Provides durable host election (flock), registration discovery, NDJSON framed transport, and the RuntimeHostConnection contract. Business handlers are injected via compositionFactory.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "fs-native-extensions": "^1.5.0"
  },
  "optionalDependencies": {
    "node-pty": "^1.1.0"
  }
}
