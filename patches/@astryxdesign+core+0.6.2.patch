diff --git a/node_modules/@astryxdesign/core/dist/Chat/ChatComposerInput.js b/node_modules/@astryxdesign/core/dist/Chat/ChatComposerInput.js
index 87dfdea..768c255 100644
--- a/node_modules/@astryxdesign/core/dist/Chat/ChatComposerInput.js
+++ b/node_modules/@astryxdesign/core/dist/Chat/ChatComposerInput.js
@@ -95,18 +95,34 @@ function selectAll(el) {
 }
 function serialize(node) {
   let result = '';
-  for (const child of Array.from(node.childNodes)) {
-    if (child.nodeType === Node.TEXT_NODE) {
-      result += child.textContent ?? '';
-    } else if (child instanceof HTMLElement) {
-      if (child.hasAttribute('data-astryx-token')) {
-        result += child.getAttribute('data-astryx-token-value') ?? '';
-      } else if (child.tagName === 'BR') {
-        result += '\n';
-      } else {
-        result += serialize(child);
+  let lastLeaf;
+  const walk = (parent) => {
+    for (const child of Array.from(parent.childNodes)) {
+      if (child.nodeType === Node.TEXT_NODE) {
+        const text = child.textContent ?? '';
+        result += text;
+        if (text) lastLeaf = child;
+      } else if (child instanceof HTMLElement) {
+        if (child.hasAttribute('data-astryx-token')) {
+          result += child.getAttribute('data-astryx-token-value') ?? '';
+          lastLeaf = child;
+        } else if (child.tagName === 'BR') {
+          result += '\n';
+          lastLeaf = child;
+        } else {
+          if ((child.tagName === 'DIV' || child.tagName === 'P') && result && !result.endsWith('\n')) {
+            result += '\n';
+          }
+          walk(child);
+        }
       }
     }
+  };
+  walk(node);
+  // Chromium keeps one terminal line break solely to place the caret.
+  if ((lastLeaf instanceof HTMLElement && lastLeaf.tagName === 'BR') ||
+      (lastLeaf?.nodeType === Node.TEXT_NODE && lastLeaf.textContent?.endsWith('\n'))) {
+    result = result.slice(0, -1);
   }
   return result;
 }
@@ -280,7 +296,7 @@ export function ChatComposerInput(props) {
       // echo before we rewrite the DOM.
       pendingEchoValueRef.current = undefined;
       const wasFocused = document.activeElement === editable;
-      editable.textContent = controlledValue;
+      editable.textContent = controlledValue.endsWith('\n') ? controlledValue + '\n' : controlledValue;
       // Setting `textContent` tears down the existing text node,
       // which collapses any Selection inside this editable to
       // offset 0. If the user was focused (e.g. a programmatic
@@ -309,9 +325,9 @@ export function ChatComposerInput(props) {
     }
     const text = serialize(editableRef.current);
     // Browsers may leave a trailing <br> when all content is deleted,
-    // which serializes to "\n". Treat whitespace-only as empty.
+    // which serialize() excludes; preserve intentional whitespace in drafts.
     const hasTokens = editableRef.current.querySelector('[data-astryx-token], [data-astryx-dictation-interim]') != null;
-    const trimmedEmpty = text.trim().length === 0 && !hasTokens;
+    const trimmedEmpty = text.length === 0 && !hasTokens;
     const nextValue = trimmedEmpty ? '' : text;
     pendingEchoValueRef.current = nextValue;
     setIsEmpty(trimmedEmpty);
@@ -505,7 +521,7 @@ export function ChatComposerInput(props) {
       return;
     }
     const files = Array.from(e.clipboardData.files);
-    if (files.length > 0) {
+    if (files.length > 0 && onFiles) {
       e.preventDefault();
       onFiles?.(files);
       return;
@@ -525,7 +541,10 @@ export function ChatComposerInput(props) {
       emitChange();
       return;
     }
-    insertTextAtCursor(editable, text);
+    // Keep plain-text paste in the native editing history (undo/redo).
+    if (!document.execCommand('insertText', false, text)) {
+      insertTextAtCursor(editable, text);
+    }
     emitChange();
   }, [onFiles, onPasteProp, emitChange, tokens, pasteAsToken]);
   const maxHeight = maxRows * LINE_HEIGHT_PX;
diff --git a/node_modules/@astryxdesign/core/dist/Chat/ChatLayout.d.ts b/node_modules/@astryxdesign/core/dist/Chat/ChatLayout.d.ts
index ff34874..a1eb352 100644
--- a/node_modules/@astryxdesign/core/dist/Chat/ChatLayout.d.ts
+++ b/node_modules/@astryxdesign/core/dist/Chat/ChatLayout.d.ts
@@ -69,6 +69,8 @@ export interface ChatLayoutProps extends BaseProps<HTMLDivElement> {
      * @default 'balanced'
      */
     density?: Density;
+  /** Disable built-in following when the host owns scrolling. */
+  autoScroll?: boolean;
 }
 export declare function ChatLayout({ children, composer, density, emptyState, scrollButton, scrollRef: externalScrollRef, xstyle, className, style, 'data-testid': testId, ref, ...rest }: ChatLayoutProps): import("react").JSX.Element;
 export declare namespace ChatLayout {
diff --git a/node_modules/@astryxdesign/core/dist/Chat/ChatLayout.js b/node_modules/@astryxdesign/core/dist/Chat/ChatLayout.js
index ff9b9fa..0457a68 100644
--- a/node_modules/@astryxdesign/core/dist/Chat/ChatLayout.js
+++ b/node_modules/@astryxdesign/core/dist/Chat/ChatLayout.js
@@ -188,6 +188,7 @@ export function ChatLayout({
   children,
   composer,
   density = 'balanced',
+  autoScroll = true,
   emptyState,
   scrollButton,
   scrollRef: externalScrollRef,
@@ -205,7 +206,8 @@ export function ChatLayout({

   // --- Default scroll behavior ---
   const scroll = useChatStreamScroll({
-    scrollRef: scrollContainerRef
+    scrollRef: scrollContainerRef,
+    enabled: autoScroll
   });
   const newMsgs = useChatNewMessages({
     isLocked: scroll.isLocked,
diff --git a/node_modules/@astryxdesign/core/src/Chat/ChatComposerInput.tsx b/node_modules/@astryxdesign/core/src/Chat/ChatComposerInput.tsx
index a45cc2f..9ab0350 100644
--- a/node_modules/@astryxdesign/core/src/Chat/ChatComposerInput.tsx
+++ b/node_modules/@astryxdesign/core/src/Chat/ChatComposerInput.tsx
@@ -287,18 +287,34 @@ function selectAll(el: HTMLElement): void {

 function serialize(node: Node): string {
   let result = '';
-  for (const child of Array.from(node.childNodes)) {
-    if (child.nodeType === Node.TEXT_NODE) {
-      result += child.textContent ?? '';
-    } else if (child instanceof HTMLElement) {
-      if (child.hasAttribute('data-astryx-token')) {
-        result += child.getAttribute('data-astryx-token-value') ?? '';
-      } else if (child.tagName === 'BR') {
-        result += '\n';
-      } else {
-        result += serialize(child);
+  let lastLeaf: Node | undefined;
+  const walk = (parent: Node) => {
+    for (const child of Array.from(parent.childNodes)) {
+      if (child.nodeType === Node.TEXT_NODE) {
+        const text = child.textContent ?? '';
+        result += text;
+        if (text) lastLeaf = child;
+      } else if (child instanceof HTMLElement) {
+        if (child.hasAttribute('data-astryx-token')) {
+          result += child.getAttribute('data-astryx-token-value') ?? '';
+          lastLeaf = child;
+        } else if (child.tagName === 'BR') {
+          result += '\n';
+          lastLeaf = child;
+        } else {
+          if ((child.tagName === 'DIV' || child.tagName === 'P') && result && !result.endsWith('\n')) {
+            result += '\n';
+          }
+          walk(child);
+        }
       }
     }
+  };
+  walk(node);
+  // Chromium keeps one terminal line break solely to place the caret.
+  if ((lastLeaf instanceof HTMLElement && lastLeaf.tagName === 'BR') ||
+      (lastLeaf?.nodeType === Node.TEXT_NODE && lastLeaf.textContent?.endsWith('\n'))) {
+    result = result.slice(0, -1);
   }
   return result;
 }
@@ -484,7 +500,7 @@ export function ChatComposerInput(props: ChatComposerInputProps) {
       // echo before we rewrite the DOM.
       pendingEchoValueRef.current = undefined;
       const wasFocused = document.activeElement === editable;
-      editable.textContent = controlledValue;
+      editable.textContent = controlledValue.endsWith('\n') ? controlledValue + '\n' : controlledValue;
       // Setting `textContent` tears down the existing text node,
       // which collapses any Selection inside this editable to
       // offset 0. If the user was focused (e.g. a programmatic
@@ -515,12 +531,12 @@ export function ChatComposerInput(props: ChatComposerInputProps) {
     }
     const text = serialize(editableRef.current);
     // Browsers may leave a trailing <br> when all content is deleted,
-    // which serializes to "\n". Treat whitespace-only as empty.
+    // which serialize() excludes; preserve intentional whitespace in drafts.
     const hasTokens =
       editableRef.current.querySelector(
         '[data-astryx-token], [data-astryx-dictation-interim]',
       ) != null;
-    const trimmedEmpty = text.trim().length === 0 && !hasTokens;
+    const trimmedEmpty = text.length === 0 && !hasTokens;
     const nextValue = trimmedEmpty ? '' : text;
     pendingEchoValueRef.current = nextValue;
     setIsEmpty(trimmedEmpty);
@@ -741,7 +757,7 @@ export function ChatComposerInput(props: ChatComposerInputProps) {
       }

       const files = Array.from(e.clipboardData.files);
-      if (files.length > 0) {
+      if (files.length > 0 && onFiles) {
         e.preventDefault();
         onFiles?.(files);
         return;
@@ -763,7 +779,10 @@ export function ChatComposerInput(props: ChatComposerInputProps) {
         return;
       }

-      insertTextAtCursor(editable, text);
+      // Keep plain-text paste in the native editing history (undo/redo).
+      if (!document.execCommand('insertText', false, text)) {
+        insertTextAtCursor(editable, text);
+      }
       emitChange();
     },
     [onFiles, onPasteProp, emitChange, tokens, pasteAsToken],
diff --git a/node_modules/@astryxdesign/core/src/Chat/ChatLayout.tsx b/node_modules/@astryxdesign/core/src/Chat/ChatLayout.tsx
index d45a912..050207f 100644
--- a/node_modules/@astryxdesign/core/src/Chat/ChatLayout.tsx
+++ b/node_modules/@astryxdesign/core/src/Chat/ChatLayout.tsx
@@ -96,6 +96,8 @@ export interface ChatLayoutProps extends BaseProps<HTMLDivElement> {
    * @default 'balanced'
    */
   density?: Density;
+  /** Disable built-in following when the host owns scrolling. */
+  autoScroll?: boolean;
 }

 // =============================================================================
@@ -272,6 +274,7 @@ export function ChatLayout({
   children,
   composer,
   density = 'balanced',
+  autoScroll = true,
   emptyState,
   scrollButton,
   scrollRef: externalScrollRef,
@@ -289,7 +292,7 @@ export function ChatLayout({
   const isSelfScrolling = !externalScrollRef;

   // --- Default scroll behavior ---
-  const scroll = useChatStreamScroll({scrollRef: scrollContainerRef});
+  const scroll = useChatStreamScroll({scrollRef: scrollContainerRef, enabled: autoScroll});
   const newMsgs = useChatNewMessages({
     isLocked: scroll.isLocked,
     onResize: scroll.scrollIfLocked,
