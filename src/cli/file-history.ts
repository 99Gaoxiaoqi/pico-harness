/**
 * @deprecated File-history command behavior now lives in `@pico/pico-host`.
 * Keep this module as a source-compatible entrypoint for older CLI callers.
 */
export * from "@pico/pico-host/file-history";
