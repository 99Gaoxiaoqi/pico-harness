/**
 * @deprecated Command availability rules now live in `@pico/cli`.
 * Keep this module as a source-compatible boundary for input-layer callers.
 */
export * from "@pico/cli/command-availability";
