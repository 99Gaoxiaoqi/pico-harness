/**
 * @deprecated The shared keybinding schema now lives in `@pico/cli`.
 * Keep this module as a source-compatible boundary for input-layer callers.
 */
export * from "@pico/cli/keybindings-schema";
